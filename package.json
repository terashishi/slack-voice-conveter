{
  "name": "slack-voice-conveter",
  "version": "1.0.0",
  "description": "",
  "main": "build/Code.js",
  "scripts": {
    "build": "tsc && cp src/appsscript.json build/",
    "push": "npm run build && clasp push",
    "deploy": "npm run push && clasp deploy",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "slack",
    "voice",
    "conveter",
    "gas",
    "google-apps-script"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/google-apps-script": "^1.0.97",
    "typescript": "^5.8.2"
  }
}